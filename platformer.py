import pygame

# constant variables
SCREEN_SIZE = (700,500)
DARK_GREY = (50,50,50)

#init
pygame.init()
screen = pygame.display.set_mode(SCREEN_SIZE)

while True:
# game loop

    # imput

    # update

    # draw
    screen.fill(DARK_GREY)
    pygame.display.flip()

# quit
pygame.quit()